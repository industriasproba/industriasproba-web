---
import Logo from './Logo.astro';
import NavLinks from './NavLinks.astro';
import ContactItem from './ContactItem.astro';

const { phone, email, address } = Astro.props;
---
<footer class="bg-neutral text-neutral-content">
  <div class="footer sm:footer-horizontal justify-between max-w-7xl mx-auto px-4 py-10">
    <aside>
      <Logo />
    </aside>
    <nav>
      <h6 class="footer-title">Navegación</h6>
      <NavLinks />
    </nav>
    <address class="not-italic flex flex-col gap-2">
      <h6 class="footer-title">Contacto</h6>
      <ContactItem name="phone" text={phone} href={`tel:${phone}`} />
      <ContactItem name="email" text={email} href={`mailto:${email}`} />
      <ContactItem
        name="address"
        text={address}
        href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(address)}`}
      />
    </address>
  </div>
  <div class="border-neutral-content/10 border-t">
    <p class="max-w-7xl mx-auto px-4 py-4 text-center text-sm text-neutral-content/40">© {new Date().getFullYear()} Industrias Proba SA. Todos los derechos reservados.</p>
  </div>
</footer>
